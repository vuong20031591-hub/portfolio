name = "portfolio"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "build/client"

# Environment variables - these will be available in context.env
# For sensitive values, use Cloudflare Dashboard Secrets instead
[vars]
# Placeholder - actual value should be set in Cloudflare Dashboard as Secret
# GITHUB_TOKEN = "your_token_here"
